#include <iostream> 
#include <vector> 
#include <stdio.h>
#include <math.h> 

using namespace std;
int main(){
    
    int N;
    cin >> N;
    int ans= sqrt(N);
    printf("The largest square has side length %d.",ans);
        
}